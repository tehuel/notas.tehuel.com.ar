# notas.tehuel.com.ar

Aplicación web (frontend HTML + backend NodeJS) para acceder a reporte de notas de Plataformas Móviles.

## Inicio rápido
- Instalar dependencias: `npm install`.
- Ejecutar en desarrollo: `npm run dev` (o `npm start`).

## Variables de entorno

En `.env.example` están las variables necesarias. Crear un archivo `.env` con los valores adecuados.